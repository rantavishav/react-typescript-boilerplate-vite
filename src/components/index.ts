export * from './layout'; // we are using * to export all the  layout components from the layout folder

export { default as Button } from './button'; // we are exporting the button component from the button folder
